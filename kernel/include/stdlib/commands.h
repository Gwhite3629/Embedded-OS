#ifndef _COMMANDS_H_
#define _COMMANDS_H_

int cat(char *buf);

int ls(char *buf);

int echo(char *buf);

int clear(char *buf);

int show_time(char *buf);

/*
int call_editor(char *buf);
*/

#endif // _COMMANDS_H_